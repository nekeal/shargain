# Frontend Guidelines

## i18n

- when adding new translations to the code, always add them to **all locale files** in the same batch — never one at a time. Missing translations display the raw key to the user, which is a bug.
